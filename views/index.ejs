<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        ul {
            display: flex;
            justify-content: space-around;
        }
    </style>
</head>

<body>
    <h1>unit converter</h1>
    <nav>
        <ul>
            <li><a href="/length">length</a></li>
            <li><a href="/weight">Weight</a></li>
        </ul>
    </nav>
    <% if(locals.mode==="length" ) { %>
        <main style="text-align: center;">
            <% if (!locals.answer) { %>
                <h2>Length converter</h2>
                <form style="display: flex; flex-direction: column; width: 80vw;" method="post" action="/handle-length"
                    target="_self">
                    <label>lengte</label>
                    <input type="number" name="length">
                    <label style="margin-top: 10px;">convert from:</label>
                    <select name="convertfrom">
                        <option value="mm">millimeter</option>
                        <option value="cm">centimeter</option>
                        <option value="m">meter</option>
                        <option value="km">kilometer</option>
                        <option value="inch">inch</option>
                        <option value="ft">foot</option>
                        <option value="yd">yard</option>
                        <option value="mile">mile</option>
                    </select>
                    <label style="margin-top: 10px;">convert to:</label>
                    <select name="convertto">
                        <option value="mm">millimeter</option>
                        <option value="cm">centimeter</option>
                        <option value="m">meter</option>
                        <option value="km">kilometer</option>
                        <option value="inch">inch</option>
                        <option value="ft">foot</option>
                        <option value="yd">yard</option>
                        <option value="mile">mile</option>
                    </select>
                    <input style="margin-top: 10px;" type="submit">
                </form>
                <% } else { %>
                    <div style="display: flex; flex-direction: column;">
                        <h2>gegeven lengte:</h2>
                        <%= locals.answer %>
                            <h2>converteer van</h2>
                            <%= locals.convertFrom %>
                                <h2>converteer naar</h2>
                                <%= locals.convertTo %>

                                    <h2>final answer:</h2>
                                    <h3>
                                        <%= locals.answer %>
                                            <%= locals.convertFrom %> equals <%= locals.finalAnswer %>
                                                    <%= locals.convertTo %>
                                    </h3>
                                    <a href="/length">reset</a>

                    </div>
                    <% } %>
        </main>
        <% } else if(locals.mode==="weight" ) {%> 
            <main style="text-align: center; ">    
                <% if(!locals.answer) { %>   
            <h2>weight converter</h2>
            <form style="display: flex; flex-direction: column; justify-content: center; width: 80vw;" method="post" action="/handle-weight">
                <label>Type in the weight</label>
                <input type="number" name="weight">
                <label style="margin-top: 10px;">convert from:</label>
                <select name="convertfrom">
                    <option value="mg">milligram</option>
                    <option value="g">gram</option>
                    <option value="kg">kilogram</option>
                    <option value="on">ounce</option>
                    <option value="po">pound</option>
                </select>
                <label style="margin-top: 10px;">convert to:</label>
                <select name="convertto">
                    <option value="mg">milligram</option>
                    <option value="g">gram</option>
                    <option value="kg">kilogram</option>
                    <option value="on">ounce</option>
                    <option value="po">pound</option>
                </select>
                <input style="margin-top: 10px;" type="submit">
            </form>
            <% } else { %>
                <div style="display: flex; flex-direction: column;">
                    <h2>gegeven gewicht:</h2>
                    <%= locals.answer %>
                        <h2>converteer van</h2>
                        <%= locals.convertFrom %>
                            <h2>converteer naar</h2>
                            <%= locals.convertTo %>

                                <h2>final answer:</h2>
                                <h3>
                                    <%= locals.answer %>
                                        <%= locals.convertFrom %> equals <%= locals.finalAnswer %>
                                                <%= locals.convertTo %>
                                </h3>
                                <a href="/weight">reset</a>

                </div>
                <% } %>
        </main>   
                <% } else { %>
                    <h2>error</h2>
                    <% } %>
</body>

</html>